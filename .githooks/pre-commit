#!/bin/bash
set -euo pipefail

cat >&2 <<'EOF'
ERROR: devkit repo hooks are now managed by prek, not core.hooksPath=.githooks.

Run:
  uv tool install prek
  git config --unset core.hooksPath
  prek install --hook-type pre-commit --hook-type pre-push
EOF

exit 1
