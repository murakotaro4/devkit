---
description: "Codex（GPT）にタスクを委譲するオーケストレーター"
argument-hint: "[topic]"
---

# /codex - Codex委譲オーケストレーター

このコマンドは `devkit:codex` スキルを呼び出します。

**使い方**: `/codex "topic"`

Skill ツールを使って `devkit:codex` スキルを実行してください。
