---
name: "goal-prompt"
description: "自律・長時間実行 / 定期ループ実行 / 大タスク完走依頼向けのゴールプロンプトを、選択肢付きインタビューで組み立てる。「ゴールプロンプトを作って」「夜間実行の指示書を作って」「ループ用プロンプトを作って」「/goal-prompt」で起動"
argument-hint: "[task]"
allowed-tools: ["Read", "Grep", "Glob", "Bash", "AskUserQuestion", "request_user_input", "spawn_agent", "wait_agent", "TaskCreate", "TaskUpdate", "Write", "Skill", "Agent"]
---

# /goal-prompt - ゴールプロンプト作成

親エージェント = インタビュー・read-only 調査・ゴールプロンプト組み立て・独立レビュー・保存・起動プロンプト提示。成果物はレビュー済みゴールファイル + 起動プロンプトであり、実行はユーザーの 1 アクションに分離する。

## 対象

$ARGUMENTS

## dig / goal-prompt 使い分け

配布先には devkit リポジトリの `AGENTS.md` が同梱されないため、この SKILL.md も使い分けの要点を保持する。軸はタスク規模ではなく自律度。

- `dig` はその場で完成させる工程。成果物は実装済み diff。ユーザーが同席し、判断をリアルタイムに供給する。
- `goal-prompt` は不在実行に耐える指示書を作る工程。成果物はレビュー済みゴールファイル + 起動プロンプト。判断を前倒しして本文へ焼き込む。
- 実装系で計画がまだ無い場合の正道は `dig` で調査・write_scope・受け入れ条件を確定し、必要ならゴール化引き継ぎでこの skill へ渡す。
- 定期ループや調査・状態確認・文書化など、実装済み diff をこの場で作らない作業は `goal-prompt` 直起動でよい。

## ハーネス判定

正本は devkit リポジトリの `AGENTS.md`「スキル共通契約」。この SKILL.md は Claude 親 / Codex 親の二層構成で実行する。要点:

- `AskUserQuestion` が使える -> Claude 親。
- なければ `spawn_agent` が使える -> Codex 親。
- どちらでもない -> 判定不能として扱い、選択肢を箇条書きで提示して自由文回答を求める。
- `request_user_input` は plan mode 依存で不安定なため、判定キーに使わない(Codex 親 plan mode の質問手段としてのみ使う)。

## タスクリスト連動

正本は devkit リポジトリの `AGENTS.md`「スキル共通契約 > タスクリスト連動」。goal-prompt 開始時に step 1-9 をタスクリストへ登録し、各 step の開始時に `in_progress`、完了時に `completed` へ更新する(Claude 親: TaskCreate / TaskUpdate、利用不可なら省略可。Codex 親: 組み込み plan 機能または通常の進捗報告で同等の進捗提示)。

## 書き込み契約

- step 1-8 は対象 repo に対して read-only。Bash は `command -v`、`git status`、`rg`、`ls`、`find`、テスト・lint コマンドの存在確認など読み取り用途だけに使う。対象 repo への Bash 書き込み、mkdir、touch、ファイル編集は行わない。
- 例外は step 7 の独立レビュー運用だけ。`mktemp` による一時領域 `JOB_DIR` の作成とレビューログ書き込みは可とする。対象 repo への書き込みは不可。
- step 9 は、ユーザーが保存を選択した場合に Write でゴールファイルを新規保存するだけ。既定パスは `docs/goals/YYYY-MM-DD-<slug>.md`。保存前にパスを確認し、同名ファイルは上書きせず `-2` などの連番にする。
- この skill は起動プロンプトを提示して終了する。cron 登録、`/schedule` 登録、実行開始、commit、push はどの step でも行わない。
- 保存前後を問わず、秘密情報、資格情報、トークン、個人情報をプロンプト本文へ転記しない。必要な場合は「環境に既に存在する設定を読む」と書き、値そのものは書かない。

## 起動プロンプトの手引き

長い本文は保存済みゴールファイルへ寄せ、起動プロンプトには短い条件文とファイル参照だけを書く。全行とも提示のみで、この skill は起動しない。

| 形態 | 提示する起動プロンプト | 扱い |
|------|------------------------|------|
| 現セッション `/goal` | `/goal docs/goals/<file>.md の成功条件を満たす or stop after <N> turns。まず docs/goals/<file>.md を読め` | 提示のみ。条件文は 4,000 字以内にし、本文はファイル参照にする |
| 別ターミナル `claude --bg` | `cd "<対象repo>" && claude --bg --permission-mode acceptEdits --allowedTools "..." "/goal <保存したゴールファイルの絶対パス> の成功条件を満たす or stop after <N> turns。まず <保存したゴールファイルの絶対パス> を読め" < /dev/null` | 提示のみ。背景セッションは別 worktree で動く場合があるため、ファイル参照は必ず絶対パスにする(未コミットの新規ファイルは相対パスだと読めない)。実装系では検証コマンドと委譲用 `Bash(codex:*)` を `--allowedTools` に含めることを推奨する |
| `/loop` | `/loop <interval> docs/goals/<file>.md を読み、進捗管理の指示に従って条件を確認する` | 提示のみ。セッション依存の巡回向け |
| `/schedule` | `/schedule <trigger> docs/goals/<file>.md を読む` | 提示のみ。登録はユーザーの 1 アクションで行う |

## 排除する失敗モード

1. 停止条件欠落: 全プロンプトに達成停止、上限停止、行き詰まり停止の 3 種を必須化する。上限停止は省略禁止。
2. ゴール誤解釈・スコープドリフト: 成功条件は機械検証(コマンド + exit code)を第一候補にする。検証コマンドがない調査・文書化系は「成果物の存在 + 客観的な手動確認手順(チェックリスト)」へ fallback する。エージェント自己申告のみの成功条件は禁止。
3. 変更範囲の膨張: 非対象、変更禁止領域、破壊的操作の扱いを本文へ明記する。
4. 無限待機: 「実行中は質問不可。曖昧な点は保守的解釈 + 判断を進捗ログに記録」と本文に焼き込む。
5. 受け渡し失敗: 長い本文はファイル参照にし、起動プロンプトには短い条件文だけを渡す。

## 質問表

1 ラウンド最大 4 問、必ず選択肢付きで聞く。親の推奨を付け、影響が小さい未知は仮定を明示して進める。Codex 親 plan mode で `request_user_input` を使う場合は 1 呼び出し最大 3 問に分ける。

| ラウンド | 目的 | 必ず聞くこと |
|----------|------|--------------|
| Round 1: 実行形態と対象 | 起動プロンプトの形と対象を決める | 形態(現セッション `/goal` / 別ターミナル `claude --bg` / `/loop` / `/schedule`)、ハーネス(Claude 親 / Codex 親 / 判定不能)、対象 repo または対象ファイル、タスク型(実装 / 調査 / 状態確認 / 文書化 / 整理) |
| Round 2: ゴール定義 | 終了状態を検証可能にする | 完了状態、検証方法、検証コマンド、品質バー |
| Round 3: 境界と停止条件 | 暴走・破壊・迷走を防ぐ | 非対象、上限停止の種類と値、行き詰まり時の扱い、破壊的操作の可否 |
| Round 4: 運用と実行戦略 | 不在実行時の観測性と実装労働の分け方を決める | 進捗の残し方、権限、途中中断と再開、完了報告の形式、実装系のみ委譲先・並列方針・effort 帯・トークン効率方針 |

## プロンプトテンプレート

生成するゴールプロンプトは次の節をこの順序で持つ。

````markdown
# Goal Prompt: <短い名前>

## 目的
<何を達成するか。背景は必要最小限>

## 成功条件(検証可能)
- <機械検証できる条件、または成果物 + 客観的な手動確認手順>

## 検証コマンド
```bash
<実行する検証コマンド。ない場合は手動確認手順を書く>
```

## 制約・非対象
- <触らないファイル、やらない作業、権限の境界>
- <破壊的操作の扱い>

## 停止条件(3 種)
- 達成停止: <成功条件を満たしたら停止>
- 上限停止: <最大ターン数、最大時間、最大ループ回数など。省略禁止>
- 行き詰まり停止: <同じ blocker が続く、外部入力が必要、検証不能など>

## 実行戦略(実装系のみ)
- 実装労働の委譲先: <codex exec / Claude サブエージェント / 親実装など>
- 並列方針: <分割可能な場合だけ並列化し、write_scope を互いに素にする>
- effort 帯: <統括・設計は高め、機械的作業は低めなど>
- トークン効率方針: <重い思考は統括のみ、作業は安いレーンへ寄せる>
- 書くのは戦略と契約のみで、逐一の手順は書かない。
- 戦略から逸脱が必要なら理由を進捗ログに記録して保守的に判断する。

## 進捗管理
- TaskCreate / TaskUpdate が使える場合はステップを登録し、開始時 in_progress、完了時 completed に更新する。
- 進捗ログ: <保存先、更新頻度、判断・blocker・検証結果の書き方>

## 実装後レビュー
- 実装系では、実装と別系統の独立レビュー(codex review 等)を実施し、指摘ゼロまで修正する。

## 実行前提
- 実行中は質問不可。曖昧な点は保守的解釈 + 判断を進捗ログに記録する。
- 秘密情報は本文に含めない。
````

## セルフチェック

step 6 で、提示前に 7 項目を必ず確認する。

1. 検証可能性: 成功条件がコマンド + exit code、または成果物 + 客観的な手動確認手順になっている。
2. 上限停止必須: 達成停止、上限停止、行き詰まり停止の 3 種があり、上限停止は省略禁止になっている。
3. 非対象・破壊的操作明記: 触らない範囲と破壊的操作の可否が明記されている。
4. 実行形態との無矛盾: 現セッション `/goal`、別ターミナル `claude --bg`、`/loop`、`/schedule` の制約と矛盾しない。
5. 秘密情報なし: 値そのものではなく参照方法だけを書いている。
6. 受け渡し制約への適合: `/goal` 条件文 4,000 字上限、長い本文はファイル参照、起動プロンプトは短い条件文だけ、という原則を満たしている。
7. 実装系ゴール: `実行戦略(実装系のみ)` と `実装後レビュー` の節があり、逸脱時ログ記録の 1 行が入っている。

## dig 引き継ぎモード

`dig` から計画コンテキスト付きで起動された場合は、Round 1-4 を「計画にない項目だけの差分確認 1 ラウンド」に圧縮する。目的 / write_scope / 受け入れ条件 / 検証コマンド / 非対象は承認済み計画から転記する。差分確認で埋めるのは、起動プロンプト形態、上限停止値、行き詰まり停止の扱い、破壊的操作の可否、権限、進捗管理、実装戦略だけとする。

commit / push 禁止と、repo ルールが独立レビューを必須とする場合の実装後レビュー要件は転記必須項目とする。差分確認後は、通常フローの組み立て + セルフチェック、独立レビュー、最終確認、保存 + 起動プロンプト提示へ進む。

## フロー

### 1. Round 1: 実行形態と対象

選択肢付き質問で、形態、ハーネス、対象 repo / ファイル、タスク型を決める。締めに目的 / 成功条件の仮説 / 非対象の仮説 / 採用した仮定を短く提示する。

### 2. 事前調査 read-only

対象 repo やファイルを read-only で確認する。`command -v claude`、`command -v codex`、テスト・lint・format コマンド、既存 docs、進捗ログ候補を調べ、保存先と検証コマンド候補を具体化する。

### 3. Round 2: ゴール定義

完了状態、検証方法、検証コマンド、品質バーを聞く。選択肢は step 2 の調査結果から具体化し、機械検証できない場合は成果物 + 手動確認チェックリストへ fallback する。

### 4. Round 3: 境界と停止条件

非対象、上限停止の種類と値、行き詰まり時の扱い、破壊的操作の可否を聞く。上限停止は省略禁止として扱い、未指定なら親の推奨値を提示して確認する。

### 5. Round 4: 運用と実行戦略

進捗の残し方、権限、途中中断と再開、完了報告の形式を聞く。実装系では、委譲先、並列方針、effort 帯、トークン効率方針を確認する。確認するのは戦略と契約だけで、実行側の逐一の作業手順は作らない。

### 6. 組み立て + セルフチェック

プロンプトテンプレートへ回答を統合し、セルフチェック 7 項目を満たすまで修正する。長い本文はファイル参照へ寄せ、`/goal` 条件文や CLI 引数を短く保つ。

### 7. ゴールプロンプト独立レビュー

完成案を独立 reviewer に渡し、成功条件・停止条件・write_scope・権限・実装戦略・レビュー要件の抜け漏れを確認する。指摘があれば step 6 に戻り、指摘ゼロになるまで修正する。

ハーネス別のレビュー手段:

- Claude 親 + codex review(第一候補):
  ```bash
  JOB_DIR=$(mktemp -d "${TMPDIR:-/tmp}/devkit-goal-review.XXXXXX") && echo "JOB_DIR=$JOB_DIR"
  JOB_DIR=<echo された記録済みのパス> && codex -a never exec -C "<対象repo>" --sandbox read-only -c model_reasoning_effort="<effort>" "<ゴール本文レビュー指示>" < /dev/null > "$JOB_DIR/review.log" 2>&1
  ```
  1 行目で echo されたパスを親が記録し、2 行目に埋め込んでから Bash `run_in_background` で起動する(シェル変数はツール呼び出し間で失われるため)。
  完了通知後に記録済み `JOB_DIR` の `review.log` を必ず読み、指摘ゼロを確認してから step 8 へ進む。指摘があれば step 6 に戻る。
- Claude 親 + codex 不在: Agent(Claude サブエージェント)へ read-only 指示、完成案、レビュー観点を渡して代替する。
- Codex 親: `spawn_agent`(explorer) に read-only 指示・完成案・レビュー観点を渡し、`wait_agent` で回収する。`spawn_agent` が使えない場合は、独立レビューを実施できないため step 8 へ進まない。完成案の全文は提示した上で、保存はせず、Claude 親での /goal-prompt 再実行、または `spawn_agent` が使える環境での再実行を案内して停止する(未レビューのままの保存・提示完了扱いはしない)。

### 8. ユーザー最終確認

独立レビュー反映後の完成プロンプト全文、保存予定パス、推奨 1 + 代替 1 の起動プロンプトを提示し、保存してよいか確認する。修正があれば step 6 に戻る。

### 9. 保存 + 起動プロンプト提示

ユーザーが保存を選んだ場合、Write でゴールファイルを新規保存する。保存後、推奨 1 + 代替 1 の起動プロンプトを提示して終了する。保存しない場合は、通常セッション貼付け用の本文だけを提示して終了する。ファイル参照型の起動プロンプトは保存済みファイルが前提のため提示しない。

## 注意

- 裸の `/goal` をトリガーとして扱わない。組み込み `/goal` との誤発火を避ける。
- `/goal` を付けるのは Claude 系の起動プロンプトだけ。委譲先 codex exec には素の実装指示を渡す。
- 非対話のコマンド例を書く場合は、stdin を `< /dev/null` で閉じる。
- codex コマンド例に `-m` でモデルを焼き込まない。モデルは config 既定に従い、調整するのは `model_reasoning_effort` だけにする。
- cron 登録、`/schedule` 登録、実行開始、commit、push は行わない。
- 実装系の実行は `dig` が正道。goal-prompt はレビュー済み指示書と起動プロンプトの作成で終了する。
